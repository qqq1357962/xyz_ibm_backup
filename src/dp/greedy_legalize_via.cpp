#include "greedy_legalize_via.h"

namespace dp {

void greedy_legalize_via(NodeData& data, DetailedPlaceDataTensor& dp_db_at, torch::Tensor node_pos_dp) {
    DetailedPlaceData dp_db(data, dp_db_at, 0, 0);
}

}  // namespace dp